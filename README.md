DO NOT USE `lodash-webpack-plugin`

There are some examples that tell you why.

### Run

```
npm i
npm run build

ls dist

node dist/some-dist-file.js
```

The source codes are written in commonjs, so you can use `node` to execute them.
